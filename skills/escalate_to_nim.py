# Orchestrates the full escalation path: runs the classifier, calls build_escalation_payload,
# converts the payload to NIM message format, calls the NVIDIA NIM API, and prints the annotated
# response.
